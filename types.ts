// types.ts
export interface MenuItem {
  id: string;
  name: string;
  description: string;
  course: string;
  price: number;
}

export interface Course {
  id: string;
  name: string;
  icon: string;
}

